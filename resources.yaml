components:
  - name: (( values.COMP_NAME ))
    version: (( values.VERSION ))
    provider:
      name: (( values.PROVIDER ))
    resources:
      - name: chart
        type: helmChart
        version: (( values.VERSION ))
        access:
          type: ociArtifact
          imageReference: (( values.CHART_URL ))
      - name: image
        type: ociArtifact
        version: (( values.VERSION ))
        access:
          type: ociArtifact
          imageReference: (( values.IMAGE ))