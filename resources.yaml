components:
  - name: (( values.COMP_NAME ))
    version: (( values.VERSION ))
    provider:
      name: (( values.PROVIDER ))
    resources:
      - name: chart
        type: helmChart
        version: (( values.VERSION ))
        input:
          type: helm
          path: (( values.CHART_URL ))
          version: (( values.VERSION ))
          repository: charts/(( values.REPO_NAME ))
      - name: image
        type: ociArtifact
        version: (( values.VERSION ))
        access:
          type: ociArtifact
          imageReference: (( values.IMAGE ))